fn main() {
    let x = 5;
    let y = 1;
    println("x is ", x);
    println("y is ", y);

    let sum = x + y;
    println("sum = x + y is ", sum);

    let multiply = x * 9;
    println("x * 9 is ", multiply);

    let divide = 10 / x;
    println("10 / x is ", divide);

    println("x - y is ", x  - y);
}